# @package _global_

reward_model: 
  name: 'sparse'
  encoder: 'dino' # default and only option is vlc
  success_bonus: 10.0

  general_training:
    dense_rewards_at_end: false
    entropy_term: "auto"
    learning_rate: 1e-3

environment:
  is_state_based: false
  ignore_language: false

