# @package _global_

defaults:
  - _self_

general_training:
  algo: bc
  action_noise: 0.1 # sigma
  sparse_only: false

model:
  policy_layer_norm: true
  critic_layer_norm: true
  pi_net_arch: [512, 512]
  qf_net_arch: [512, 512, 512]

offline_training:
  offline_training_steps: 20000 

online_training:
  total_time_steps: 0